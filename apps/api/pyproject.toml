[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[project]
name = "agentops-api"
version = "0.1.0"
description = "AgentOps Control Tower — FastAPI backend"
requires-python = ">=3.11"
